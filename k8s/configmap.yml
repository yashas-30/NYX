apiVersion: v1
kind: ConfigMap
metadata:
  name: nyx-config
  namespace: nyx-production
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  CACHE_TTL: "86400"
  MAX_REQUESTS_PER_MINUTE: "100"
