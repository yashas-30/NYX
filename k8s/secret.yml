apiVersion: v1
kind: Secret
metadata:
  name: nyx-secrets
  namespace: nyx-production
type: Opaque
stringData:
# WARNING: Replace NYX_MASTER_KEY before deploying to production.
# Use External Secrets Operator or Sealed Secrets — never commit real keys.
# Validate: CI will fail if this placeholder is still present in production.
  NYX_MASTER_KEY: "replace-me-in-production-with-sealed-secrets"
