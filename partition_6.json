[
  {
    "instances": [
      {
        "file": "server/features/agents/agents.router.ts",
        "start_line": 58,
        "end_line": 66,
        "start_col": 4,
        "end_col": 26,
        "fragment": "return res.status(400).json({ error: 'Model is required' });\r\n  }\r\n\r\n  res.setHeader('Content-Type', 'text/event-stream');\r\n  res.setHeader('Cache-Control', 'no-cache');\r\n  res.setHeader('Connection', 'keep-alive');\r\n  res.setHeader('X-Accel-Buffering', 'no');\r\n  res.flushHeaders();\r\n  sendSseTokenRotate(res);"
      },
      {
        "file": "server/features/ai-providers/gemini.router.ts",
        "start_line": 39,
        "end_line": 48,
        "start_col": 4,
        "end_col": 26,
        "fragment": "return res.status(400).json({ error: 'Model is required' });\r\n  }\r\n\r\n  // Set event-stream headers immediately\r\n  res.setHeader('Content-Type', 'text/event-stream');\r\n  res.setHeader('Cache-Control', 'no-cache');\r\n  res.setHeader('Connection', 'keep-alive');\r\n  res.setHeader('X-Accel-Buffering', 'no');\r\n  res.flushHeaders();\r\n  sendSseTokenRotate(res);"
      }
    ],
    "token_count": 70,
    "line_count": 10,
    "fingerprint": "dup:51812b5c",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (10 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  }
]