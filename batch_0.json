[
  {
    "instances": [
      {
        "file": ".github/skills/ck/commands/forget.mjs",
        "start_line": 16,
        "end_line": 28,
        "start_col": 74,
        "end_col": 51,
        "fragment": "'./shared.mjs';\r\n\r\nconst arg = process.argv[2];\r\nconst cwd = process.env.PWD || process.cwd();\r\n\r\nconst resolved = resolveContext(arg, cwd);\r\nif (!resolved) {\r\n  const hint = arg ? `No project matching \"${arg}\".` : 'This directory is not registered.';\r\n  console.log(`${hint}`);\r\n  process.exit(1);\r\n}\r\n\r\nconst { name, contextDir, projectPath } = resolved;"
      },
      {
        "file": ".github/skills/ck/commands/resume.mjs",
        "start_line": 12,
        "end_line": 24,
        "start_col": 50,
        "end_col": 42,
        "fragment": "'./shared.mjs';\r\n\r\nconst arg = process.argv[2];\r\nconst cwd = process.env.PWD || process.cwd();\r\n\r\nconst resolved = resolveContext(arg, cwd);\r\nif (!resolved) {\r\n  const hint = arg ? `No project matching \"${arg}\".` : 'This directory is not registered.';\r\n  console.log(`${hint} Run /ck:init to register it.`);\r\n  process.exit(1);\r\n}\r\n\r\nconst { context, projectPath } = resolved;"
      }
    ],
    "token_count": 74,
    "line_count": 13,
    "fingerprint": "dup:db3651bf",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (13 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": ".github/skills/ck/commands/forget.mjs",
        "start_line": 16,
        "end_line": 26,
        "start_col": 74,
        "end_col": 1,
        "fragment": "'./shared.mjs';\r\n\r\nconst arg = process.argv[2];\r\nconst cwd = process.env.PWD || process.cwd();\r\n\r\nconst resolved = resolveContext(arg, cwd);\r\nif (!resolved) {\r\n  const hint = arg ? `No project matching \"${arg}\".` : 'This directory is not registered.';\r\n  console.log(`${hint}`);\r\n  process.exit(1);\r\n}"
      },
      {
        "file": ".github/skills/ck/commands/info.mjs",
        "start_line": 11,
        "end_line": 21,
        "start_col": 48,
        "end_col": 1,
        "fragment": "'./shared.mjs';\r\n\r\nconst arg = process.argv[2];\r\nconst cwd = process.env.PWD || process.cwd();\r\n\r\nconst resolved = resolveContext(arg, cwd);\r\nif (!resolved) {\r\n  const hint = arg ? `No project matching \"${arg}\".` : 'This directory is not registered.';\r\n  console.log(`${hint} Run /ck:init to register it.`);\r\n  process.exit(1);\r\n}"
      },
      {
        "file": ".github/skills/ck/commands/resume.mjs",
        "start_line": 12,
        "end_line": 22,
        "start_col": 50,
        "end_col": 1,
        "fragment": "'./shared.mjs';\r\n\r\nconst arg = process.argv[2];\r\nconst cwd = process.env.PWD || process.cwd();\r\n\r\nconst resolved = resolveContext(arg, cwd);\r\nif (!resolved) {\r\n  const hint = arg ? `No project matching \"${arg}\".` : 'This directory is not registered.';\r\n  console.log(`${hint} Run /ck:init to register it.`);\r\n  process.exit(1);\r\n}"
      }
    ],
    "token_count": 73,
    "line_count": 11,
    "fingerprint": "dup:adf8c03b",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (11 lines, 3 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/admin/admin.router.ts",
        "start_line": 104,
        "end_line": 113,
        "start_col": 24,
        "end_col": 62,
        "fragment": ", (req, res) => {\r\n  const adminKey = process.env.ADMIN_KEY;\r\n  if (adminKey) {\r\n    const clientKey = (req.headers['x-admin-key'] || req.query.adminKey) as string | undefined;\r\n    if (!clientKey || !safeCompare(clientKey, adminKey)) {\r\n      return res.status(401).json({ error: 'Unauthorized: Invalid admin key' });\r\n    }\r\n  }\r\n  try {\r\n    const days = parseInt(String(req.query.days || '30'), 10);"
      },
      {
        "file": "server/features/admin/admin.router.ts",
        "start_line": 172,
        "end_line": 181,
        "start_col": 24,
        "end_col": 46,
        "fragment": ", (req, res) => {\r\n  const adminKey = process.env.ADMIN_KEY;\r\n  if (adminKey) {\r\n    const clientKey = (req.headers['x-admin-key'] || req.query.adminKey) as string | undefined;\r\n    if (!clientKey || !safeCompare(clientKey, adminKey)) {\r\n      return res.status(401).json({ error: 'Unauthorized: Invalid admin key' });\r\n    }\r\n  }\r\n  try {\r\n    const memoryUsage = process.memoryUsage();"
      }
    ],
    "token_count": 80,
    "line_count": 10,
    "fingerprint": "dup:6934be3c",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (10 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/conversations/conversations.router.ts",
        "start_line": 35,
        "end_line": 43,
        "start_col": 53,
        "end_col": 20,
        "fragment": ");\r\n    if (!ok) return res.status(404).json({ error: 'Folder not found' });\r\n    res.json({ ok: true });\r\n  } catch (error: any) {\r\n    res.status(500).json({ error: error.message });\r\n  }\r\n});\r\n\r\nconversationsRouter."
      },
      {
        "file": "server/features/conversations/conversations.router.ts",
        "start_line": 45,
        "end_line": 53,
        "start_col": 47,
        "end_col": 20,
        "fragment": ");\r\n    if (!ok) return res.status(404).json({ error: 'Folder not found' });\r\n    res.json({ ok: true });\r\n  } catch (error: any) {\r\n    res.status(500).json({ error: error.message });\r\n  }\r\n});\r\n\r\nconversationsRouter."
      }
    ],
    "token_count": 67,
    "line_count": 9,
    "fingerprint": "dup:621b7693",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/model-proxy/modelProxy.router.ts",
        "start_line": 9,
        "end_line": 15,
        "start_col": 29,
        "end_col": 62,
        "fragment": ", validate(modelQuerySchema), async (req, res) => {\n  const { provider, apiKey } = req.body;\n  if (apiKey && !service.validateKey(provider, apiKey)) {\n    return res.status(400).json({ error: 'Invalid API key format for provider: ' + provider });\n  }\n  try {\n    const models = await service.listModels(provider, apiKey);"
      },
      {
        "file": "server/features/model-proxy/modelProxy.router.ts",
        "start_line": 22,
        "end_line": 28,
        "start_col": 30,
        "end_col": 59,
        "fragment": ", validate(modelQuerySchema), async (req, res) => {\n  const { provider, apiKey } = req.body;\n  if (apiKey && !service.validateKey(provider, apiKey)) {\n    return res.status(400).json({ error: 'Invalid API key format for provider: ' + provider });\n  }\n  try {\n    const quota = await service.getQuota(provider, apiKey);"
      }
    ],
    "token_count": 66,
    "line_count": 7,
    "fingerprint": "dup:43bfbb22",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (7 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/workspace/workspaceIntelligence.ts",
        "start_line": 15,
        "end_line": 27,
        "start_col": 0,
        "end_col": 1,
        "fragment": "export interface WorkspaceProfile {\n  rootPath: string;\n  projectType: 'react' | 'node' | 'python' | 'rust' | 'go' | 'arduino' | 'generic';\n  packageManager: 'npm' | 'yarn' | 'pnpm' | 'pip' | 'cargo' | 'poetry' | null;\n  entryPoints: string[];\n  keyDependencies: Record<string, string>;\n  directoryTree: string;\n  testFramework: 'vitest' | 'jest' | 'pytest' | 'cargo-test' | null;\n  lintConfig: 'eslint' | 'biome' | 'ruff' | null;\n  typescriptConfig: any | null;\n  recentGitCommits: string[];\n  openFiles: string[];\n}"
      },
      {
        "file": "src/types/inference.ts",
        "start_line": 63,
        "end_line": 75,
        "start_col": 0,
        "end_col": 1,
        "fragment": "export interface WorkspaceProfile {\r\n  rootPath: string;\r\n  projectType: 'react' | 'node' | 'python' | 'rust' | 'go' | 'arduino' | 'generic';\r\n  packageManager: 'npm' | 'yarn' | 'pnpm' | 'pip' | 'cargo' | 'poetry' | null;\r\n  entryPoints: string[];\r\n  keyDependencies: Record<string, string>;\r\n  directoryTree: string;\r\n  testFramework: 'vitest' | 'jest' | 'pytest' | 'cargo-test' | null;\r\n  lintConfig: 'eslint' | 'biome' | 'ruff' | null;\r\n  typescriptConfig: any | null;\r\n  recentGitCommits: string[];\r\n  openFiles: string[];\r\n}"
      }
    ],
    "token_count": 71,
    "line_count": 13,
    "fingerprint": "dup:b237405f",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (13 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/middleware/requestSigner.ts",
        "start_line": 14,
        "end_line": 25,
        "start_col": 13,
        "end_col": 26,
        "fragment": "requestSignerMiddleware = (\r\n  req: express.Request,\r\n  res: express.Response,\r\n  next: express.NextFunction\r\n) => {\r\n  // Skip signing check for public/health routes\r\n  const fullPath = req.originalUrl.split('?')[0].replace(/\\/$/, '');\r\n  const isPublic = new Set([\r\n    '/api/v1/health',\r\n    '/api/v1/vault/status',\r\n    '/api/v1/vault/token',\r\n    '/api/v1/auth/session'"
      },
      {
        "file": "server.ts",
        "start_line": 457,
        "end_line": 467,
        "start_col": 8,
        "end_col": 28,
        "fragment": "sessionValidationMiddleware = (\r\n    req: express.Request,\r\n    res: express.Response,\r\n    next: express.NextFunction\r\n  ) => {\r\n    const fullPath = req.originalUrl.split('?')[0].replace(/\\/$/, '');\r\n    const isPublic = new Set([\r\n      '/api/v1/health',\r\n      '/api/v1/vault/status',\r\n      '/api/v1/vault/token',\r\n      '/api/v1/auth/session'"
      }
    ],
    "token_count": 57,
    "line_count": 12,
    "fingerprint": "dup:eb86ad19",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (12 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server.ts",
        "start_line": 230,
        "end_line": 237,
        "start_col": 73,
        "end_col": 18,
        "fragment": ", {\r\n        signal: controller.signal,\r\n      }).finally(() => clearTimeout(timeout));\r\n      if (!res.ok) {\r\n        throw new Error(`Scrapling health check returned ${res.status}`);\r\n      }\r\n    } catch {\r\n      logger.warn("
      },
      {
        "file": "server.ts",
        "start_line": 255,
        "end_line": 262,
        "start_col": 75,
        "end_col": 18,
        "fragment": ", {\r\n        signal: controller.signal,\r\n      }).finally(() => clearTimeout(timeout));\r\n      if (!res.ok) {\r\n        throw new Error(`Antigravity health check returned ${res.status}`);\r\n      }\r\n    } catch {\r\n      logger.warn("
      }
    ],
    "token_count": 50,
    "line_count": 8,
    "fingerprint": "dup:90caf3d3",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (8 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/chat/components/ChatHeader.tsx",
        "start_line": 95,
        "end_line": 103,
        "start_col": 0,
        "end_col": 34,
        "fragment": "function formatLatency(ms: number): string {\r\n  if (ms <= 0) return '—';\r\n  if (ms < 1000) return `${ms}ms`;\r\n  if (ms >= 60000) {\r\n    const mins = Math.floor(ms / 60000);\r\n    const secs = ((ms % 60000) / 1000).toFixed(1);\r\n    return `${mins}m ${secs}s`;\r\n  }\r\n  return `${(ms / 1000).toFixed(1)"
      },
      {
        "file": "src/features/coder/components/CoderHeader.tsx",
        "start_line": 41,
        "end_line": 49,
        "start_col": 0,
        "end_col": 34,
        "fragment": "function formatLatency(ms: number): string {\r\n  if (ms <= 0) return '—';\r\n  if (ms < 1000) return `${ms}ms`;\r\n  if (ms >= 60000) {\r\n    const mins = Math.floor(ms / 60000);\r\n    const secs = ((ms % 60000) / 1000).toFixed(1);\r\n    return `${mins}m ${secs}s`;\r\n  }\r\n  return `${(ms / 1000).toFixed(1)"
      }
    ],
    "token_count": 80,
    "line_count": 9,
    "fingerprint": "dup:80aa373c",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/chat/components/ChatHeader.tsx",
        "start_line": 367,
        "end_line": 405,
        "start_col": 36,
        "end_col": 5,
        "fragment": ");\r\n  const [scraplingStatus, setScraplingStatus] = useState<\r\n    'checking' | 'running' | 'restarting' | 'offline'\r\n  >('checking');\r\n\r\n  // Poll scrapling status\r\n  useEffect(() => {\r\n    let active = true;\r\n    const checkScrapling = async () => {\r\n      try {\r\n        const res = await fetchWithAuth('/api/v1/admin/scrapling-status');\r\n        if (!active) return;\r\n        if (res.ok) {\r\n          const data = await res.json();\r\n          setScraplingStatus(data.status || 'offline');\r\n        } else {\r\n          setScraplingStatus('offline');\r\n        }\r\n      } catch {\r\n        if (!active) return;\r\n        setScraplingStatus('offline');\r\n      }\r\n    };\r\n    checkScrapling();\r\n    const interval = setInterval(checkScrapling, 30_000);\r\n    return () => {\r\n      active = false;\r\n      clearInterval(interval);\r\n    };\r\n  }, []);\r\n\r\n  // Live timer during generation\r\n  useEffect(() => {\r\n    if (isLoading) {\r\n      const start = Date.now();\r\n      setLiveElapsed(0);\r\n      const interval = setInterval(() => setLiveElapsed(Date.now() - start), 50);\r\n      return () => clearInterval(interval);\r\n    }"
      },
      {
        "file": "src/features/coder/components/CoderHeader.tsx",
        "start_line": 69,
        "end_line": 107,
        "start_col": 52,
        "end_col": 5,
        "fragment": ");\r\n  const [scraplingStatus, setScraplingStatus] = useState<\r\n    'checking' | 'running' | 'restarting' | 'offline'\r\n  >('checking');\r\n\r\n  useEffect(() => {\r\n    let active = true;\r\n    const checkScrapling = async () => {\r\n      try {\r\n        const res = await fetchWithAuth('/api/v1/admin/scrapling-status');\r\n        if (!active) return;\r\n        if (res.ok) {\r\n          const data = await res.json();\r\n          setScraplingStatus(data.status || 'offline');\r\n        } else {\r\n          setScraplingStatus('offline');\r\n        }\r\n      } catch {\r\n        if (!active) return;\r\n        setScraplingStatus('offline');\r\n      }\r\n    };\r\n    checkScrapling();\r\n    const interval = setInterval(checkScrapling, 30_000);\r\n    return () => {\r\n      active = false;\r\n      clearInterval(interval);\r\n    };\r\n  }, []);\r\n\r\n  useEffect(() => {\r\n    if (isLoading) {\r\n      const start = Date.now();\r\n      setLiveElapsed(0);\r\n      const interval = setInterval(() => {\r\n        setLiveElapsed(Date.now() - start);\r\n      }, 50);\r\n      return () => clearInterval(interval);\r\n    }"
      }
    ],
    "token_count": 198,
    "line_count": 39,
    "fingerprint": "dup:44f60186",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (39 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/coder/pipeline/index.ts",
        "start_line": 229,
        "end_line": 238,
        "start_col": 10,
        "end_col": 53,
        "fragment": "finalTokens = result.metrics.tokens;\r\n    const finalTps = finalElapsed > 0 ? Math.round(finalTokens / (finalElapsed / 1000)) : 0;\r\n    const finalMetrics = { latency: finalElapsed, tokens: finalTokens, tps: finalTps };\r\n\r\n    updateHistory((prev) => {\r\n      const h = [...prev];\r\n      const last = h[h.length - 1];\r\n      if (last && last.role === 'assistant') {\r\n        last.status = 'success';\r\n        last.content = accumulatedOutput + resultText"
      },
      {
        "file": "src/features/coder/pipeline/index.ts",
        "start_line": 340,
        "end_line": 349,
        "start_col": 8,
        "end_col": 39,
        "fragment": "finalTokens = countTokens(finalSummaryOutput);\r\n  const finalTps = finalElapsed > 0 ? Math.round(finalTokens / (finalElapsed / 1000)) : 0;\r\n  const finalMetrics = { latency: finalElapsed, tokens: finalTokens, tps: finalTps };\r\n\r\n  updateHistory((prev) => {\r\n    const h = [...prev];\r\n    const last = h[h.length - 1];\r\n    if (last && last.role === 'assistant') {\r\n      last.status = 'success';\r\n      last.content = finalSummaryOutput"
      }
    ],
    "token_count": 83,
    "line_count": 10,
    "fingerprint": "dup:34659d38",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (10 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/settings/components/ApiKeyVault.tsx",
        "start_line": 111,
        "end_line": 119,
        "start_col": 4,
        "end_col": 20,
        "fragment": "if (!rememberKeys) {\r\n      // Save keys ephemerally to Zustand in-memory state\r\n      for (const provider of Object.keys(keysInput)) {\r\n        const val = keysInput[provider];\r\n        if (val !== undefined && val.trim().length > 0) {\r\n          await updateApiKey(provider, val);\r\n        }\r\n      }\r\n      toast.success("
      },
      {
        "file": "src/features/settings/components/ApiKeyVault.tsx",
        "start_line": 130,
        "end_line": 137,
        "start_col": 6,
        "end_col": 22,
        "fragment": "if (res.ok) {\r\n        for (const provider of Object.keys(keysInput)) {\r\n          const val = keysInput[provider];\r\n          if (val !== undefined && val.trim().length > 0) {\r\n            await updateApiKey(provider, val);\r\n          }\r\n        }\r\n        toast.success("
      }
    ],
    "token_count": 57,
    "line_count": 9,
    "fingerprint": "dup:d8d3bc6e",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  }
]