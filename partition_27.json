[
  {
    "instances": [
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 132,
        "end_line": 136,
        "start_col": 47,
        "end_col": 58,
        "fragment": ");\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      },
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 156,
        "end_line": 160,
        "start_col": 55,
        "end_col": 58,
        "fragment": ");\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      },
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 168,
        "end_line": 172,
        "start_col": 55,
        "end_col": 58,
        "fragment": ");\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      }
    ],
    "token_count": 59,
    "line_count": 5,
    "fingerprint": "dup:880d9c2b",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (5 lines, 3 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 132,
        "end_line": 136,
        "start_col": 14,
        "end_col": 58,
        "fragment": "thinkingSteps.push(event.content!);\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      },
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 144,
        "end_line": 148,
        "start_col": 14,
        "end_col": 58,
        "fragment": "accumulatedText += event.content!;\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      },
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 156,
        "end_line": 160,
        "start_col": 14,
        "end_col": 58,
        "fragment": "artifacts.push(event.metadata as Artifact);\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      },
      {
        "file": "src/core/agents/useChatAgent.ts",
        "start_line": 168,
        "end_line": 172,
        "start_col": 14,
        "end_col": 58,
        "fragment": "citations.push(event.metadata as Citation);\r\n              setMessages((prev) => {\r\n                const next = [...prev];\r\n                const idx = next.findIndex((m) => m.id === assistantId);\r\n                if (idx !== -1) next[idx] = { ...next[idx]"
      }
    ],
    "token_count": 57,
    "line_count": 5,
    "fingerprint": "dup:54f52286",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (5 lines, 4 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  }
]