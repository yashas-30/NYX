[
  {
    "instances": [
      {
        "file": "server/fastify/fastify.server.ts",
        "start_line": 76,
        "end_line": 84,
        "start_col": 29,
        "end_col": 53,
        "fragment": ", async (request, reply) => {\r\n    const { provider } = request.body as any;\r\n    if (!provider || !adapters[provider]) {\r\n      return reply.code(400).send({ error: 'Unsupported provider' });\r\n    }\r\n\r\n    // Load balancer gives us a key to use for fetching models\r\n    const apiKey = keyManager.getNextKey(provider);\r\n    const cacheKey = modelCache.generateKey(provider,"
      },
      {
        "file": "server/fastify/fastify.server.ts",
        "start_line": 97,
        "end_line": 104,
        "start_col": 30,
        "end_col": 53,
        "fragment": ", async (request, reply) => {\r\n    const { provider } = request.body as any;\r\n    if (!provider || !adapters[provider]) {\r\n      return reply.code(400).send({ error: 'Unsupported provider' });\r\n    }\r\n\r\n    const apiKey = keyManager.getNextKey(provider);\r\n    const cacheKey = modelCache.generateKey(provider,"
      }
    ],
    "token_count": 71,
    "line_count": 9,
    "fingerprint": "dup:f53f6854",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  }
]