[
  {
    "instances": [
      {
        "file": "server/fastify/adapters/gemini.adapter.ts",
        "start_line": 35,
        "end_line": 67,
        "start_col": 25,
        "end_col": 24,
        "fragment": "{\r\n      contents,\r\n      generationConfig: {\r\n        temperature: request.temperature ?? 0.7,\r\n      },\r\n    };\r\n\r\n    const res = await fetch(url, {\r\n      method: 'POST',\r\n      headers: {\r\n        'Content-Type': 'application/json',\r\n      },\r\n      body: JSON.stringify(payload),\r\n    });\r\n\r\n    if (!res.ok) {\r\n      throw new Error(`Gemini API Error ${res.status}: ${res.statusText}`);\r\n    }\r\n\r\n    const reader = res.body!.getReader();\r\n    const decoder = new TextDecoder('utf-8');\r\n\r\n    try {\r\n      while (true) {\r\n        const { done, value } = await reader.read();\r\n        if (done) break;\r\n        const chunk = decoder.decode(value, { stream: true });\r\n\r\n        const lines = chunk.split('\\n');\r\n        for (const line of lines) {\r\n          if (line.startsWith('data: ')) {\r\n            const dataStr = line.replace('data: ', '').trim();\r\n            if (!dataStr"
      },
      {
        "file": "server/fastify/adapters/ollama.adapter.ts",
        "start_line": 26,
        "end_line": 59,
        "start_col": 20,
        "end_col": 24,
        "fragment": "{\n      model,\n      messages: request.messages,\n      stream: true,\n      temperature: request.temperature ?? 0.7,\n      max_tokens: request.max_tokens,\n    };\n\n    const res = await fetch(url, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify(payload),\n    });\n\n    if (!res.ok) {\n      throw new Error(`Ollama API Error ${res.status}: ${res.statusText}. Ensure Ollama is running.`);\n    }\n\n    const reader = res.body!.getReader();\n    const decoder = new TextDecoder('utf-8');\n\n    try {\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        const chunk = decoder.decode(value, { stream: true });\n\n        const lines = chunk.split('\\n');\n        for (const line of lines) {\n          if (line.startsWith('data: ')) {\n            const dataStr = line.replace('data: ', '').trim();\n            if (!dataStr"
      }
    ],
    "token_count": 165,
    "line_count": 34,
    "fingerprint": "dup:02fecceb",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (34 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/fastify/adapters/lmstudio.adapter.ts",
        "start_line": 12,
        "end_line": 23,
        "start_col": 39,
        "end_col": 40,
        "fragment": "`lmstudio/${m.id}`);\n    } catch {\n      return [];\n    }\n  }\n\n  async getQuota(apiKey?: string): Promise<any> {\n    return { status: 'ok', type: 'local_unlimited' };\n  }\n\n  async *streamChat(request: ChatRequest, apiKey?: string): AsyncGenerator<string, void, unknown> {\n    const model = request.model.replace("
      },
      {
        "file": "server/fastify/adapters/ollama.adapter.ts",
        "start_line": 11,
        "end_line": 22,
        "start_col": 41,
        "end_col": 40,
        "fragment": "`ollama/${m.name}`);\n    } catch {\n      return [];\n    }\n  }\n\n  async getQuota(apiKey?: string): Promise<any> {\n    return { status: 'ok', type: 'local_unlimited' };\n  }\n\n  async *streamChat(request: ChatRequest, apiKey?: string): AsyncGenerator<string, void, unknown> {\n    const model = request.model.replace("
      }
    ],
    "token_count": 59,
    "line_count": 12,
    "fingerprint": "dup:c28bc254",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (12 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/fastify/adapters/lmstudio.adapter.ts",
        "start_line": 25,
        "end_line": 44,
        "start_col": 10,
        "end_col": 74,
        "fragment": "url = `http://127.0.0.1:${port}/v1/chat/completions`;\n\n    const payload = {\n      model,\n      messages: request.messages,\n      stream: true,\n      temperature: request.temperature ?? 0.7,\n      max_tokens: request.max_tokens,\n    };\n\n    const res = await fetch(url, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify(payload),\n    });\n\n    if (!res.ok) {\n      throw new Error(`LM Studio API Error ${res.status}: ${res.statusText"
      },
      {
        "file": "server/fastify/adapters/ollama.adapter.ts",
        "start_line": 24,
        "end_line": 43,
        "start_col": 10,
        "end_col": 71,
        "fragment": "url = 'http://127.0.0.1:11434/v1/chat/completions';\n\n    const payload = {\n      model,\n      messages: request.messages,\n      stream: true,\n      temperature: request.temperature ?? 0.7,\n      max_tokens: request.max_tokens,\n    };\n\n    const res = await fetch(url, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify(payload),\n    });\n\n    if (!res.ok) {\n      throw new Error(`Ollama API Error ${res.status}: ${res.statusText"
      }
    ],
    "token_count": 72,
    "line_count": 20,
    "fingerprint": "dup:b431db3f",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (20 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/fastify/adapters/lmstudio.adapter.ts",
        "start_line": 44,
        "end_line": 76,
        "start_col": 129,
        "end_col": 5,
        "fragment": ");\n    }\n\n    const reader = res.body!.getReader();\n    const decoder = new TextDecoder('utf-8');\n\n    try {\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        const chunk = decoder.decode(value, { stream: true });\n\n        const lines = chunk.split('\\n');\n        for (const line of lines) {\n          if (line.startsWith('data: ')) {\n            const dataStr = line.replace('data: ', '').trim();\n            if (!dataStr || dataStr === '[DONE]') continue;\n\n            try {\n              const data = JSON.parse(dataStr);\n              const content = data.choices?.[0]?.delta?.content;\n              if (content) {\n                yield content;\n              }\n            } catch (err) {\n              // ignore parse errors for partial chunks\n            }\n          }\n        }\n      }\n    } finally {\n      reader.releaseLock();\n    }"
      },
      {
        "file": "server/fastify/adapters/ollama.adapter.ts",
        "start_line": 43,
        "end_line": 75,
        "start_col": 100,
        "end_col": 5,
        "fragment": ");\n    }\n\n    const reader = res.body!.getReader();\n    const decoder = new TextDecoder('utf-8');\n\n    try {\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        const chunk = decoder.decode(value, { stream: true });\n\n        const lines = chunk.split('\\n');\n        for (const line of lines) {\n          if (line.startsWith('data: ')) {\n            const dataStr = line.replace('data: ', '').trim();\n            if (!dataStr || dataStr === '[DONE]') continue;\n\n            try {\n              const data = JSON.parse(dataStr);\n              const content = data.choices?.[0]?.delta?.content;\n              if (content) {\n                yield content;\n              }\n            } catch (err) {\n              // ignore parse errors for partial chunks\n            }\n          }\n        }\n      }\n    } finally {\n      reader.releaseLock();\n    }"
      }
    ],
    "token_count": 176,
    "line_count": 33,
    "fingerprint": "dup:91ba99c6",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (33 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/ai-providers/shared.schema.ts",
        "start_line": 23,
        "end_line": 33,
        "start_col": 7,
        "end_col": 83,
        "fragment": "const aiSettingsSchema = z\n  .object({\n    temperature: z.number().min(0).max(2).optional(),\n    maxTokens: z.number().int().min(1).max(131072).optional(),\n    topP: z.number().min(0).max(1).optional(),\n    topK: z.number().int().min(1).max(100).optional(),\n    stream: z.boolean().optional(),\n  })\n  .optional();\n\nexport const gatewayUrlsSchema = z.record(z.string(), z.string().url()).optional();"
      },
      {
        "file": "server/features/cache/cache.schema.ts",
        "start_line": 8,
        "end_line": 23,
        "start_col": 0,
        "end_col": 3,
        "fragment": "const aiSettingsSchema = z\n  .object({\n    temperature: z.number().min(0).max(2).optional(),\n    maxTokens: z.number().int().min(1).max(131072).optional(),\n    topP: z.number().min(0).max(1).optional(),\n    topK: z.number().int().min(1).max(100).optional(),\n    stream: z.boolean().optional(),\n  })\n  .optional();\n\nexport const cacheSetSchema = z.object({\n  key: z.string().min(1).max(2048),\n  data: z.any(),\n  provider: z.string().min(1).max(64),\n  model: z.string().min(1).max(128),\n});"
      }
    ],
    "token_count": 124,
    "line_count": 16,
    "fingerprint": "dup:ad48a693",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (16 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/cache/cache.schema.ts",
        "start_line": 1,
        "end_line": 16,
        "start_col": 0,
        "end_col": 13,
        "fragment": "import { z } from 'zod';\n\nconst chatMessageSchema = z.object({\n  role: z.enum(['user', 'assistant', 'system', 'model']),\n  content: z.string().max(10 * 1024 * 1024),\n});\n\nconst aiSettingsSchema = z\n  .object({\n    temperature: z.number().min(0).max(2).optional(),\n    maxTokens: z.number().int().min(1).max(131072).optional(),\n    topP: z.number().min(0).max(1).optional(),\n    topK: z.number().int().min(1).max(100).optional(),\n    stream: z.boolean().optional(),\n  })\n  .optional()"
      },
      {
        "file": "server/features/local-models/localModels.schema.ts",
        "start_line": 1,
        "end_line": 16,
        "start_col": 0,
        "end_col": 13,
        "fragment": "import { z } from 'zod';\r\n\r\nconst chatMessageSchema = z.object({\r\n  role: z.enum(['user', 'assistant', 'system', 'model']),\r\n  content: z.string().max(10 * 1024 * 1024),\r\n});\r\n\r\nconst aiSettingsSchema = z\r\n  .object({\r\n    temperature: z.number().min(0).max(2).optional(),\r\n    maxTokens: z.number().int().min(1).max(131072).optional(),\r\n    topP: z.number().min(0).max(1).optional(),\r\n    topK: z.number().int().min(1).max(100).optional(),\r\n    stream: z.boolean().optional(),\r\n  })\r\n  .optional()"
      }
    ],
    "token_count": 169,
    "line_count": 16,
    "fingerprint": "dup:121ad5fd",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (16 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/local-models/localModelRunner.ts",
        "start_line": 63,
        "end_line": 69,
        "start_col": 40,
        "end_col": 16,
        "fragment": ", () => {\r\n      const port = (server.address() as net.AddressInfo).port;\r\n      server.close(() => resolve(port));\r\n    });\r\n    server.on('error', (err: any) => {\r\n      if (err.code === 'EADDRINUSE') {\r\n        resolve("
      },
      {
        "file": "server/lib/localServer.ts",
        "start_line": 19,
        "end_line": 25,
        "start_col": 29,
        "end_col": 18,
        "fragment": ", () => {\n        const port = (server.address() as net.AddressInfo).port;\n        server.close(() => resolve(port));\n      });\n      server.on('error', (err: any) => {\n        if (err.code === 'EADDRINUSE') {\n          resolve("
      }
    ],
    "token_count": 62,
    "line_count": 7,
    "fingerprint": "dup:0e6b8752",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (7 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/local-models/localModelRunner.ts",
        "start_line": 361,
        "end_line": 366,
        "start_col": 10,
        "end_col": 46,
        "fragment": "for (const line of lines) {\r\n            const match = line.match(/^\\s*(Device|Vulkan|CUDA)\\s*(\\d+)\\s*:?/i);\r\n            if (match) {\r\n              const type = match[1].toLowerCase();\r\n              const idxStr = match[2];\r\n              const idx = parseInt(idxStr, 10)"
      },
      {
        "file": "server/features/local-models/localModelRunner.ts",
        "start_line": 381,
        "end_line": 386,
        "start_col": 10,
        "end_col": 46,
        "fragment": "for (const line of lines) {\r\n            const match = line.match(/^\\s*(Device|Vulkan|CUDA)\\s*(\\d+)\\s*:?/i);\r\n            if (match) {\r\n              const type = match[1].toLowerCase();\r\n              const idxStr = match[2];\r\n              const idx = parseInt(idxStr, 10)"
      }
    ],
    "token_count": 56,
    "line_count": 6,
    "fingerprint": "dup:3ff2f10a",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (6 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/local-models/localModelRunner.ts",
        "start_line": 696,
        "end_line": 706,
        "start_col": 38,
        "end_col": 47,
        "fragment": ");\r\n        \r\n        // Ensure llama-server.exe is in the root of BIN_DIR\r\n        const findBinRecursive = (dir: string): string | null => {\r\n          const files = fs.readdirSync(dir);\r\n          for (const f of files) {\r\n            const p = path.join(dir, f);\r\n            if (fs.statSync(p).isDirectory()) {\r\n              const res = findBinRecursive(p);\r\n              if (res) return res;\r\n            } else if (f === 'llama-server.exe'"
      },
      {
        "file": "server/features/local-models/localModelRunner.ts",
        "start_line": 717,
        "end_line": 727,
        "start_col": 55,
        "end_col": 43,
        "fragment": ");\r\n        \r\n        // Find llama-server recursively\r\n        const findBinRecursive = (dir: string): string | null => {\r\n          const files = fs.readdirSync(dir);\r\n          for (const f of files) {\r\n            const p = path.join(dir, f);\r\n            if (fs.statSync(p).isDirectory()) {\r\n              const res = findBinRecursive(p);\r\n              if (res) return res;\r\n            } else if (f === 'llama-server'"
      }
    ],
    "token_count": 86,
    "line_count": 11,
    "fingerprint": "dup:de657e89",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (11 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/nyx/nyx.router.ts",
        "start_line": 29,
        "end_line": 35,
        "start_col": 14,
        "end_col": 17,
        "fragment": "('/subagent-status', (req, res) => {\r\n  try {\r\n    const token = req.headers['x-nyx-session-token'] as string | undefined;\r\n    if (!token) {\r\n      return res.status(401).json({ error: 'Missing x-nyx-session-token header' });\r\n    }\r\n    const { tasks"
      },
      {
        "file": "server/features/nyx/nyx.router.ts",
        "start_line": 48,
        "end_line": 54,
        "start_col": 13,
        "end_col": 15,
        "fragment": "('/subagent-status', (req, res) => {\r\n  try {\r\n    const token = req.headers['x-nyx-session-token'] as string | undefined;\r\n    if (!token) {\r\n      return res.status(401).json({ error: 'Missing x-nyx-session-token header' });\r\n    }\r\n    const tasks"
      }
    ],
    "token_count": 53,
    "line_count": 7,
    "fingerprint": "dup:4b891505",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (7 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/features/nyx/nyx.router.ts",
        "start_line": 40,
        "end_line": 48,
        "start_col": 13,
        "end_col": 14,
        "fragment": "{ success: true });\r\n  } catch (e: unknown) {\r\n    const msg = e instanceof Error ? e.message : String(e);\r\n    res.status(500).json({ error: msg });\r\n  }\r\n});\r\n\r\n// GET /api/nyx/subagent-status\r\nnyxRouter.get("
      },
      {
        "file": "server/features/nyx/nyx.router.ts",
        "start_line": 55,
        "end_line": 63,
        "start_col": 13,
        "end_col": 14,
        "fragment": "{ success: true, tasks });\r\n  } catch (e: unknown) {\r\n    const msg = e instanceof Error ? e.message : String(e);\r\n    res.status(500).json({ error: msg });\r\n  }\r\n});\r\n\r\n// GET /api/nyx/rules\r\nnyxRouter.get("
      }
    ],
    "token_count": 52,
    "line_count": 9,
    "fingerprint": "dup:208959d7",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "server/lib/logger.ts",
        "start_line": 10,
        "end_line": 24,
        "start_col": 27,
        "end_col": 22,
        "fragment": ": string {\n  if (process.env.NYX_WORKSPACE_ROOT) {\n    return path.resolve(process.env.NYX_WORKSPACE_ROOT);\n  }\n  let dir =\n    typeof __dirname !== 'undefined' ? __dirname : path.dirname(fileURLToPath(import.meta.url));\n  for (let i = 0; i < 5; i++) {\n    if (fs.existsSync(path.join(dir, 'package.json'))) {\n      return dir;\n    }\n    const parent = path.dirname(dir);\n    if (parent === dir) break;\n    dir = parent;\n  }\n  return process.cwd()"
      },
      {
        "file": "server/lib/paths.ts",
        "start_line": 12,
        "end_line": 26,
        "start_col": 26,
        "end_col": 22,
        "fragment": ": string {\n  if (process.env.NYX_WORKSPACE_ROOT) {\n    return path.resolve(process.env.NYX_WORKSPACE_ROOT);\n  }\n  let dir =\n    typeof __dirname !== 'undefined' ? __dirname : path.dirname(fileURLToPath(import.meta.url));\n  for (let i = 0; i < 5; i++) {\n    if (fs.existsSync(path.join(dir, 'package.json'))) {\n      return dir;\n    }\n    const parent = path.dirname(dir);\n    if (parent === dir) break;\n    dir = parent;\n  }\n  return process.cwd()"
      }
    ],
    "token_count": 114,
    "line_count": 15,
    "fingerprint": "dup:16e6c2ae",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (15 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/core/prompts/chatPrompts.ts",
        "start_line": 316,
        "end_line": 333,
        "start_col": 2,
        "end_col": 3,
        "fragment": "parts.push(`<safety>\r\nRefuse requests involving:\r\n- Illegal activities, violence, self-harm\r\n- Generation of malware, exploits, or harmful code\r\n- Creation of deceptive content (deepfakes, scams)\r\n- Private information about non-public individuals\r\n\r\nRefusal format: \"I can't help with that because [brief reason]. I'd be happy to help with [alternative].\"\r\n</safety>`);\r\n\r\n  // ── Dynamic APO Directives (Highest Priority) ─────────────────────────────\r\n\r\n  if (context.lightningDirectives && context.lightningDirectives.length > 0) {\r\n    parts.push(`<directives priority=\"maximum\">\r\nThe following user-optimized directives override general instructions:\r\n${context.lightningDirectives.map((d, i) => `${i + 1}. ${d}`).join('\\n')}\r\n</directives>`);\r\n  }"
      },
      {
        "file": "src/core/prompts/coderPrompts.ts",
        "start_line": 1064,
        "end_line": 1075,
        "start_col": 2,
        "end_col": 3,
        "fragment": "parts.push(`CRITICAL ANTI-HALLUCINATION & GROUNDING GUARDRAILS (WEIGHT: MAXIMUM):\r\n1. Strictly ground all generated code, functions, configurations, and variables in the verified codebase facts and search context. Do NOT guess or make up folder structures, imported libraries, methods, or third-party packages.\r\n2. Under no circumstances should you generate speculative code placeholders or \"TODO\" notes in the body of implementations. If a function is requested, provide its COMPLETE, syntactically correct implementation.\r\n3. If any essential information, parameters, or dependency paths are missing, explicitly refuse to guess or write dummy implementations. Instead, specify the exact missing components and request them.\r\n4. Verify all import paths, variable declarations, and type signatures. Do not assume APIs or models exist unless verified in context.\r\n5. NEVER include internal chain-of-thought, reasoning traces, or drafting steps in your final response. CRITICAL: You MUST NOT start your response with phrases like \"The user said\", \"The user wants\", \"I will\", \"Here is\", or \"This is\". Do NOT start by analyzing the user's intent or explaining what you will do. Provide the final, structured answer or code block IMMEDIATELY. DO NOT wrap your entire response in a markdown code block (like \\`\\`\\`text or \\`\\`\\`markdown) unless you are ONLY outputting code. Just output regular markdown directly. Keep responses as concise and direct as possible to conserve tokens.`);\r\n\r\n  if (context.lightningDirectives && context.lightningDirectives.length > 0) {\r\n    parts.push(`[CONTINUOUS LEARNING: DYNAMIC APO DIRECTIVES ACTIVE]\r\nThe following dynamic prompt directives have been optimized from real user reinforcement feedback. Treat them with HIGHEST behavioral weight (Priority multiplier: 2.0x) over default coding conventions:\r\n${context.lightningDirectives.map((d, i) => `Directive #${i + 1}: ${d}`).join('\\n')}`);\r\n  }"
      }
    ],
    "token_count": 61,
    "line_count": 18,
    "fingerprint": "dup:a1a6845b",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (18 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/chat/hooks/useChatLogic.ts",
        "start_line": 18,
        "end_line": 34,
        "start_col": 25,
        "end_col": 61,
        "fragment": "{\r\n  apiKeys: Record<string, string>;\r\n  modelSettings: any;\r\n  trackUsage: (provider: string, tokens: number) => void;\r\n  models?: Record<'nyx', string>;\r\n  setModel?: (modelId: string) => void;\r\n  chatSessions: any;\r\n  lightningEnabled?: boolean;\r\n  lightningDirectives?: string[];\r\n  logRollout?: (\r\n    agentType: 'chat' | 'coder',\r\n    task: string,\r\n    response: string,\r\n    spans?: any[],\r\n    initialReward?: number | null\r\n  ) => string;\r\n  submitReward?: (rolloutId: string, reward: number) => void;"
      },
      {
        "file": "src/features/coder/hooks/useCoderLogic.ts",
        "start_line": 25,
        "end_line": 41,
        "start_col": 26,
        "end_col": 61,
        "fragment": "{\r\n  apiKeys: Record<string, string>;\r\n  modelSettings: any;\r\n  trackUsage: (provider: string, tokens: number) => void;\r\n  models?: Record<'nyx', string>;\r\n  setModel?: (modelId: string) => void;\r\n  chatSessions: any;\r\n  lightningEnabled?: boolean;\r\n  lightningDirectives?: string[];\r\n  logRollout?: (\r\n    agentType: 'chat' | 'coder',\r\n    task: string,\r\n    response: string,\r\n    spans?: any[],\r\n    initialReward?: number | null\r\n  ) => string;\r\n  submitReward?: (rolloutId: string, reward: number) => void;"
      }
    ],
    "token_count": 81,
    "line_count": 17,
    "fingerprint": "dup:46e7f1ef",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (17 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/chat/hooks/useChatLogic.ts",
        "start_line": 173,
        "end_line": 180,
        "start_col": 0,
        "end_col": 13,
        "fragment": "function areMessagesEqual(a: ChatMessage[], b: ChatMessage[]): boolean {\r\n  if (a.length !== b.length) return false;\r\n  for (let i = 0; i < a.length; i++) {\r\n    if (a[i].role !== b[i].role) return false;\r\n    if (a[i].content !== b[i].content) return false;\r\n    if (a[i].status !== b[i].status) return false;\r\n  }\r\n  return true"
      },
      {
        "file": "src/features/coder/hooks/useCoderLogic.ts",
        "start_line": 15,
        "end_line": 22,
        "start_col": 0,
        "end_col": 13,
        "fragment": "function areMessagesEqual(a: ChatMessage[], b: ChatMessage[]): boolean {\r\n  if (a.length !== b.length) return false;\r\n  for (let i = 0; i < a.length; i++) {\r\n    if (a[i].role !== b[i].role) return false;\r\n    if (a[i].content !== b[i].content) return false;\r\n    if (a[i].status !== b[i].status) return false;\r\n  }\r\n  return true"
      }
    ],
    "token_count": 102,
    "line_count": 8,
    "fingerprint": "dup:4b273a44",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (8 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/chat/hooks/useChatLogic.ts",
        "start_line": 202,
        "end_line": 217,
        "start_col": 2,
        "end_col": 61,
        "fragment": "const [localModels, setLocalModels] = useState<Record<'nyx', string>>({ nyx: '' });\r\n  const models = propModels ?? localModels;\r\n\r\n  const setModel = useCallback(\r\n    (mid: string) => {\r\n      if (propSetModel) {\r\n        propSetModel(mid);\r\n      } else {\r\n        setLocalModels({ nyx: mid });\r\n      }\r\n    },\r\n    [propSetModel]\r\n  );\r\n\r\n  // --- History with reducer for atomic updates ---\r\n  const [history, dispatch] = useReducer(historyReducer, []);"
      },
      {
        "file": "src/features/coder/hooks/useAgentState.ts",
        "start_line": 19,
        "end_line": 34,
        "start_col": 2,
        "end_col": 98,
        "fragment": "const [localModels, setLocalModels] = useState<Record<'nyx', string>>({\r\n    nyx: '',\r\n  });\r\n  const models = propModels ?? localModels;\r\n  const setModel = useCallback(\r\n    (mid: string) => {\r\n      if (propSetModel) {\r\n        propSetModel(mid);\r\n      } else {\r\n        setLocalModels({ nyx: mid });\r\n      }\r\n    },\r\n    [propSetModel]\r\n  );\r\n\r\n  const [agentPersonas, setAgentPersonas] = useState<Record<'nyx', AgentPersona>>(DEFAULT_AGENTS);"
      }
    ],
    "token_count": 65,
    "line_count": 16,
    "fingerprint": "dup:6444350e",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (16 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/chat/hooks/useChatLogic.ts",
        "start_line": 546,
        "end_line": 555,
        "start_col": 43,
        "end_col": 25,
        "fragment": ".images\r\n        ?.map((img) => ({\r\n          name: img.name,\r\n          mimeType: img.mimeType || 'image/jpeg',\r\n          data: img.data || img.dataUrl || img.url || '',\r\n        }))\r\n        .filter((img) => !!img.data);\r\n\r\n      // Auto-regenerate assistant response\r\n      runChatRef.current?"
      },
      {
        "file": "src/features/chat/hooks/useChatLogic.ts",
        "start_line": 576,
        "end_line": 584,
        "start_col": 34,
        "end_col": 25,
        "fragment": ".images\r\n        ?.map((img) => ({\r\n          name: img.name,\r\n          mimeType: img.mimeType || 'image/jpeg',\r\n          data: img.data || img.dataUrl || img.url || '',\r\n        }))\r\n        .filter((img) => !!img.data);\r\n\r\n      runChatRef.current?"
      }
    ],
    "token_count": 62,
    "line_count": 10,
    "fingerprint": "dup:eb38bf8c",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (10 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/coder/hooks/useCoderLogic.ts",
        "start_line": 196,
        "end_line": 213,
        "start_col": 56,
        "end_col": 14,
        "fragment": ");\r\n\r\n        let sid = null;\r\n        if (chatSessions?.createSession) {\r\n          sid = chatSessions.createSession(newHistory);\r\n          if (sid && chatSessions.setActiveSession) {\r\n            chatSessions.setActiveSession(sid);\r\n          }\r\n        }\r\n        messagesRef.current = newHistory;\r\n        setLocalMessages(newHistory);\r\n        if (sid) {\r\n          activeSidRef.current = sid;\r\n          createdSessionIdRef.current = sid;\r\n        }\r\n        setTimeout(() => {\r\n          runCoder?.(newPrompt);\r\n        }, 0);"
      },
      {
        "file": "src/features/coder/hooks/useCoderLogic.ts",
        "start_line": 221,
        "end_line": 237,
        "start_col": 59,
        "end_col": 12,
        "fragment": ");\r\n      let sid = null;\r\n      if (chatSessions?.createSession) {\r\n        sid = chatSessions.createSession(newHistory);\r\n        if (sid && chatSessions.setActiveSession) {\r\n          chatSessions.setActiveSession(sid);\r\n        }\r\n      }\r\n      messagesRef.current = newHistory;\r\n      setLocalMessages(newHistory);\r\n      if (sid) {\r\n        activeSidRef.current = sid;\r\n        createdSessionIdRef.current = sid;\r\n      }\r\n      setTimeout(() => {\r\n        runCoder(newPrompt);\r\n      }, 0);"
      }
    ],
    "token_count": 90,
    "line_count": 18,
    "fingerprint": "dup:48d4eb77",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (18 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/model-registry/components/ModelRegistryPage.tsx",
        "start_line": 4,
        "end_line": 12,
        "start_col": 33,
        "end_col": 1,
        "fragment": "{\r\n  models?: Record<'nyx', string>;\r\n  selectModel?: (modelId: string) => void;\r\n  apiKeys: Record<string, string>;\r\n  providerStatuses?: Record<string, 'online' | 'offline' | 'no-key'>;\r\n  activeMode?: 'coder' | 'registry' | 'settings';\r\n  setActiveMode?: (mode: 'coder' | 'registry' | 'settings') => void;\r\n  sidebarOpen?: boolean;\r\n}"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 20,
        "end_line": 28,
        "start_col": 33,
        "end_col": 1,
        "fragment": "{\r\n  models?: Record<'nyx', string>;\r\n  selectModel?: (modelId: string) => void;\r\n  apiKeys: Record<string, string>;\r\n  providerStatuses?: Record<string, 'online' | 'offline' | 'no-key'>;\r\n  activeMode?: 'coder' | 'registry' | 'settings';\r\n  setActiveMode?: (mode: 'coder' | 'registry' | 'settings') => void;\r\n  sidebarOpen?: boolean;\r\n}"
      }
    ],
    "token_count": 50,
    "line_count": 9,
    "fingerprint": "dup:03c7dc03",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 182,
        "end_line": 228,
        "start_col": 33,
        "end_col": 4,
        "fragment": "); // Keep open to monitor progress\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Auto-setup failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Auto-setup error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleDownloadAllCompatible = async () => {\r\n    const allCompatCount = compatibility?.allCompatibleModelIds?.length || 0;\r\n    if (allCompatCount === 0) {\r\n      toast.error('No compatible GGUF presets detected for your system.');\r\n      return;\r\n    }\r\n    if (\r\n      !confirm(\r\n        `Queue and download all ${allCompatCount} compatible models on your device? This requires significant disk space.`\r\n      )\r\n    )\r\n      return;\r\n\r\n    setActionInProgress('download-all-compatible');\r\n    try {\r\n      const res = await AIService.fetchWithAuth(\r\n        '/api/v1/nyx/local-models/download-all-compatible',\r\n        {\r\n          method: 'POST',\r\n        }\r\n      );\r\n      if (res.ok) {\r\n        const data = await res.json();\r\n        toast.success(data.message || 'Bulk downloads initiated.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Bulk download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Bulk download error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 290,
        "end_line": 319,
        "start_col": 26,
        "end_col": 4,
        "fragment": ");\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed to start: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handlePause = async (modelId: string) => {\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/pause', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Download paused. Resume to continue.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Pause failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 388,
        "end_line": 419,
        "start_col": 26,
        "end_col": 4,
        "fragment": ");\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to load model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleStop = async (modelId: string) => {\r\n    setActionInProgress(modelId);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/stop', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Model unloaded from Resident RAM. Memory released.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to unload model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      }
    ],
    "token_count": 55,
    "line_count": 47,
    "fingerprint": "dup:420da7ee",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (47 lines, 3 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 217,
        "end_line": 278,
        "start_col": 65,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Bulk download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Bulk download error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleCustomUrlDownload = async () => {\r\n    if (!customUrl.trim()) {\r\n      toast.error('Please enter a valid URL.');\r\n      return;\r\n    }\r\n    if (!customUrl.startsWith('http://') && !customUrl.startsWith('https://')) {\r\n      toast.error('URL must start with http:// or https://');\r\n      return;\r\n    }\r\n\r\n    const urlStr = customUrl.trim();\r\n    if (!urlStr.toLowerCase().split('?')[0].endsWith('.gguf')) {\r\n      toast.error('URL must point to a .gguf file');\r\n      return;\r\n    }\r\n\r\n    try {\r\n      const allowedDomains = ['huggingface.co', 'github.com', 'modelscope.cn'];\r\n      const parsedUrl = new URL(urlStr);\r\n      if (!allowedDomains.some((d) => parsedUrl.hostname.includes(d))) {\r\n        toast.warning('Untrusted domain. Proceed with caution.');\r\n      }\r\n    } catch (e) {\r\n      toast.error('Invalid URL format');\r\n      return;\r\n    }\r\n\r\n    setActionInProgress(urlStr);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/download', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId: customUrl.trim() }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Custom URL download started successfully.');\r\n        setCustomUrl('');\r\n        setShowDownloadModal(false);\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 289,
        "end_line": 319,
        "start_col": 61,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed to start: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handlePause = async (modelId: string) => {\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/pause', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Download paused. Resume to continue.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Pause failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 387,
        "end_line": 419,
        "start_col": 62,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to load model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleStop = async (modelId: string) => {\r\n    setActionInProgress(modelId);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/stop', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Model unloaded from Resident RAM. Memory released.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to unload model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      }
    ],
    "token_count": 60,
    "line_count": 62,
    "fingerprint": "dup:c2f09c0ab44564c7-2",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (62 lines, 3 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 217,
        "end_line": 278,
        "start_col": 65,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Bulk download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Bulk download error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleCustomUrlDownload = async () => {\r\n    if (!customUrl.trim()) {\r\n      toast.error('Please enter a valid URL.');\r\n      return;\r\n    }\r\n    if (!customUrl.startsWith('http://') && !customUrl.startsWith('https://')) {\r\n      toast.error('URL must start with http:// or https://');\r\n      return;\r\n    }\r\n\r\n    const urlStr = customUrl.trim();\r\n    if (!urlStr.toLowerCase().split('?')[0].endsWith('.gguf')) {\r\n      toast.error('URL must point to a .gguf file');\r\n      return;\r\n    }\r\n\r\n    try {\r\n      const allowedDomains = ['huggingface.co', 'github.com', 'modelscope.cn'];\r\n      const parsedUrl = new URL(urlStr);\r\n      if (!allowedDomains.some((d) => parsedUrl.hostname.includes(d))) {\r\n        toast.warning('Untrusted domain. Proceed with caution.');\r\n      }\r\n    } catch (e) {\r\n      toast.error('Invalid URL format');\r\n      return;\r\n    }\r\n\r\n    setActionInProgress(urlStr);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/download', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId: customUrl.trim() }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Custom URL download started successfully.');\r\n        setCustomUrl('');\r\n        setShowDownloadModal(false);\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 289,
        "end_line": 319,
        "start_col": 61,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed to start: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handlePause = async (modelId: string) => {\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/pause', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Download paused. Resume to continue.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Pause failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 387,
        "end_line": 442,
        "start_col": 62,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to load model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleStop = async (modelId: string) => {\r\n    setActionInProgress(modelId);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/stop', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Model unloaded from Resident RAM. Memory released.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to unload model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      }
    ],
    "token_count": 60,
    "line_count": 62,
    "fingerprint": "dup:c2f09c0ab44564c7-2",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (62 lines, 3 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 217,
        "end_line": 319,
        "start_col": 65,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Bulk download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Bulk download error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleCustomUrlDownload = async () => {\r\n    if (!customUrl.trim()) {\r\n      toast.error('Please enter a valid URL.');\r\n      return;\r\n    }\r\n    if (!customUrl.startsWith('http://') && !customUrl.startsWith('https://')) {\r\n      toast.error('URL must start with http:// or https://');\r\n      return;\r\n    }\r\n\r\n    const urlStr = customUrl.trim();\r\n    if (!urlStr.toLowerCase().split('?')[0].endsWith('.gguf')) {\r\n      toast.error('URL must point to a .gguf file');\r\n      return;\r\n    }\r\n\r\n    try {\r\n      const allowedDomains = ['huggingface.co', 'github.com', 'modelscope.cn'];\r\n      const parsedUrl = new URL(urlStr);\r\n      if (!allowedDomains.some((d) => parsedUrl.hostname.includes(d))) {\r\n        toast.warning('Untrusted domain. Proceed with caution.');\r\n      }\r\n    } catch (e) {\r\n      toast.error('Invalid URL format');\r\n      return;\r\n    }\r\n\r\n    setActionInProgress(urlStr);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/download', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId: customUrl.trim() }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Custom URL download started successfully.');\r\n        setCustomUrl('');\r\n        setShowDownloadModal(false);\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 387,
        "end_line": 442,
        "start_col": 62,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to load model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleStop = async (modelId: string) => {\r\n    setActionInProgress(modelId);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/stop', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Model unloaded from Resident RAM. Memory released.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to unload model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      }
    ],
    "token_count": 60,
    "line_count": 62,
    "fingerprint": "dup:c2f09c0ab44564c7-4",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (62 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 217,
        "end_line": 319,
        "start_col": 65,
        "end_col": 4,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Bulk download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Bulk download error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleCustomUrlDownload = async () => {\r\n    if (!customUrl.trim()) {\r\n      toast.error('Please enter a valid URL.');\r\n      return;\r\n    }\r\n    if (!customUrl.startsWith('http://') && !customUrl.startsWith('https://')) {\r\n      toast.error('URL must start with http:// or https://');\r\n      return;\r\n    }\r\n\r\n    const urlStr = customUrl.trim();\r\n    if (!urlStr.toLowerCase().split('?')[0].endsWith('.gguf')) {\r\n      toast.error('URL must point to a .gguf file');\r\n      return;\r\n    }\r\n\r\n    try {\r\n      const allowedDomains = ['huggingface.co', 'github.com', 'modelscope.cn'];\r\n      const parsedUrl = new URL(urlStr);\r\n      if (!allowedDomains.some((d) => parsedUrl.hostname.includes(d))) {\r\n        toast.warning('Untrusted domain. Proceed with caution.');\r\n      }\r\n    } catch (e) {\r\n      toast.error('Invalid URL format');\r\n      return;\r\n    }\r\n\r\n    setActionInProgress(urlStr);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/download', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n        body: JSON.stringify({ modelId: customUrl.trim() }),\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Custom URL download started successfully.');\r\n        setCustomUrl('');\r\n        setShowDownloadModal(false);\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Download failed: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      },
      {
        "file": "src/features/model-registry/components/ModelRegistryView.tsx",
        "start_line": 387,
        "end_line": 444,
        "start_col": 62,
        "end_col": 37,
        "fragment": ");\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to load model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };\r\n\r\n  const handleStop = async (modelId: string) => {\r\n    setActionInProgress(modelId);\r\n    try {\r\n      const res = await AIService.fetchWithAuth('/api/v1/nyx/local-models/stop', {\r\n        method: 'POST',\r\n        headers: { 'Content-Type': 'application/json' },\r\n      });\r\n      if (res.ok) {\r\n        toast.success('Model unloaded from Resident RAM. Memory released.');\r\n        fetchNativeModels();\r\n      } else {\r\n        const errData = await res.json();\r\n        toast.error(`Failed to unload model: ${errData.error}`);\r\n      }\r\n    } catch (error: any) {\r\n      toast.error(`Error: ${error.message}`);\r\n    } finally {\r\n      setActionInProgress(null);\r\n    }\r\n  };"
      }
    ],
    "token_count": 60,
    "line_count": 62,
    "fingerprint": "dup:c2f09c0ab44564c7-4",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (62 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  },
  {
    "instances": [
      {
        "file": "src/main.tsx",
        "start_line": 48,
        "end_line": 55,
        "start_col": 33,
        "end_col": 66,
        "fragment": ")) {\r\n    const match = trimmed.match(/oklab\\(([^)]+)\\)/);\r\n    if (match) {\r\n      const parts = match[1].split('/');\r\n      const coords = parts[0].trim().split(/\\s+/).map(parseVal);\r\n      const alpha = parts[1] ? parseVal(parts[1].trim()) : 1;\r\n      if (coords.length === 3 && !coords.some(isNaN)) {\r\n        return oklabToRgb(coords[0], coords[1], coords[2], alpha);"
      },
      {
        "file": "src/main.tsx",
        "start_line": 60,
        "end_line": 67,
        "start_col": 33,
        "end_col": 66,
        "fragment": ")) {\r\n    const match = trimmed.match(/oklch\\(([^)]+)\\)/);\r\n    if (match) {\r\n      const parts = match[1].split('/');\r\n      const coords = parts[0].trim().split(/\\s+/).map(parseVal);\r\n      const alpha = parts[1] ? parseVal(parts[1].trim()) : 1;\r\n      if (coords.length === 3 && !coords.some(isNaN)) {\r\n        return oklchToRgb(coords[0], coords[1], coords[2], alpha);"
      }
    ],
    "token_count": 100,
    "line_count": 8,
    "fingerprint": "dup:5852b3ca",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (8 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  }
]