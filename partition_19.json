[
  {
    "instances": [
      {
        "file": "server/features/vault/vault.service.ts",
        "start_line": 123,
        "end_line": 131,
        "start_col": 35,
        "end_col": 16,
        "fragment": "token: string | undefined): boolean {\r\n  if (!token) return false;\r\n  \r\n  const tokenHash = crypto.createHash('sha256').update(token).digest('hex');\r\n  \r\n  try {\r\n    const session = db.select().from(sessions).where(eq(sessions.tokenHash, tokenHash)).get();\r\n    \r\n    if (!session"
      },
      {
        "file": "server/features/vault/vault.service.ts",
        "start_line": 151,
        "end_line": 159,
        "start_col": 36,
        "end_col": 16,
        "fragment": "token: string | undefined): boolean {\r\n  if (!token) return false;\r\n  \r\n  const tokenHash = crypto.createHash('sha256').update(token).digest('hex');\r\n  \r\n  try {\r\n    const session = db.select().from(sessions).where(eq(sessions.tokenHash, tokenHash)).get();\r\n    \r\n    if (!session"
      }
    ],
    "token_count": 75,
    "line_count": 9,
    "fingerprint": "dup:bd08c44a",
    "actions": [
      {
        "type": "extract-shared",
        "auto_fixable": false,
        "description": "Extract duplicated code (9 lines, 2 instances) into a shared function"
      },
      {
        "type": "suppress-line",
        "auto_fixable": false,
        "description": "Suppress with an inline comment above the duplicated code",
        "comment": "// fallow-ignore-next-line code-duplication"
      }
    ]
  }
]