# Express port (default 3000)
PORT=3000

# Fastify streaming port (default 3001)
FASTIFY_PORT=3001

# Override DNS (NOT recommended for VPNs/corporate networks)
# NYX_OVERRIDE_DNS=true

# Admin log key (unset = disabled)
# ADMIN_KEY=your-secret-key

# Node environment
NODE_ENV=development
