version: '3.8'
services:
  nyx-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: always

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
